# 角色与任务
你现在是一名顶尖的前端工程师与 UX 专家。你需要基于我已经确定的设计规范，为我构建一个【AI行业自媒体个人网站】。

# 输入与上下文
1. 请首先读取我项目根目录下的 `DESIGN.md` 文件，严格遵循其中的色彩变量、字体、组件风格和动效约束（基调：极简画廊编辑风）。
2. 请读取 `.cursor/rules/project-context.mdc` 以了解网站的核心定位与4大内容版块。

# 构建要求
请创建并实现以下页面/区块结构（可通过单页平滑滚动串联，也可规划为多页路由）：

1. **Hero Section (首屏)**：
   - 大面积留白，使用优雅的衬线体与无衬线体混排。
   - 包含一句话个人定位（如："探索 AI 边界，记录真实世界"），以及一个极简的向下滚动提示动画。

2. **Articles (我的文章)**：
   - 采用类似杂志目录的极简纯文本列表。
   - 交互：鼠标悬停 (Hover) 时显示细腻的横线动画，并在光标旁或侧边柔和浮现文章的配图预览。

3. **AI Works (AI作品区)**：
   - 采用非对称画廊网格 (Asymmetrical Grid) 展示高质量的 AI 生成图像或项目卡片。
   - 交互：图片进入视口时带柔和的 Fade In Up；滚动时带有平滑的图片内部视差 (Parallax)。

4. **Photography (摄影集)**：
   - 提供横向滚动 (Horizontal Scroll) 或打破常规网格的大画幅错落排列，凸显影像本身的呼吸感和质感。

5. **About (个人介绍)**：
   - 采用纸质杂志封底式的极简排版，包含个人理念、经历标签与干净的联系方式链接。

# 技术栈约束与规范
1. 请使用本项目的基础技术栈（建议使用 Next.js + Tailwind CSS + Framer Motion，如我在对话中提出了其他框架，请以我的提议为准）。
2. **严禁在代码中写死十六进制色号 (Hex)**，必须将 `DESIGN.md` 中的色彩提取为全局 CSS 变量 (如 `var(--color-bg)`) 并在 Tailwind 中配置。
3. 必须保证页面性能，复杂动效不可导致滚动卡顿，移动端需做适度降级处理。

# 执行指令
请确认你已清楚上述上下文。确认后，请告诉我你打算采用的技术栈，并一步一步开始为我生成框架、全局配置以及各页面的代码。每次生成新区块前请向我说明。